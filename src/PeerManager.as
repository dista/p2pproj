package  
{
	/**
     * ...
     * @author dista
     * 
     * this class manage all peers, such as 
     * - connect to peer 
     * - listen for peer
     * - peer exit
     * - if peer count is too low, query more peer and connect
     * 
     */
    public class PeerManager 
    {
        
        public function PeerManager() 
        {
            
        }
        
    }

}